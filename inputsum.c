#include<stdio.h>
int main()
{
    int a,b;
    printf("enter the value of a and b that you want");
    scanf("%d%d",&a,&b);
     int sum=(a+b);
    printf("the value of sum is:%d",sum);
    return 0;
}